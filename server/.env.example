## NuVision News — Server Environment
# Copy this file to `server/.env` and fill in values.

# Hugging Face API key for the local inference proxy (optional).
# Required only for server-side summaries/NER/embeddings. Get one at:
# https://huggingface.co/settings/tokens
HF_API_KEY=

# Optional admin token to protect admin endpoints. Set a random string to enable
# `/admin/*` endpoints for cache stats and clearing.
ADMIN_TOKEN=
